export const goToFiltered = (navigate) => {
  navigate(`/filtrados`);
};

export const goToHome = (navigate) => {
  navigate("/");
};

export const goToSeasonPage = (navigate) => {
  navigate("episodios");
};
